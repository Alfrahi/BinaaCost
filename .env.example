# Vite env (client-visible). Keep one per line.

# PocketBase (dev)
VITE_POCKETBASE_URL=http://127.0.0.1:8090

# Supabase (legacy — remove after migration complete)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=

# Admin bootstrap (server-only; not VITE_ prefixed so Vite doesn't expose it)
# PB_ADMIN_EMAIL=admin@example.com
# PB_ADMIN_PASSWORD=changeme